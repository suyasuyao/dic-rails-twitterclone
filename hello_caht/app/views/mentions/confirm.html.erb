<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @mention, url: mentions_path, local: true) do |form| %>
<%= form.hidden_field :content %>
<p>Content:<%= @mention.content %></p>
<%= form.submit  "登録する" %>
<%= form.submit "戻る", name: 'back' %>
<% end %>